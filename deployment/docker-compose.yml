version: '3.8'

services:
  blog:
    build: .
    container_name: binbin-blog
    ports:
      - "80:80"
    restart: always
    # 注意：在静态博客架构中，Volume 挂载 Markdown 仅在开发环境有效。
    # 生产环境部署时，由于代码被 Vite 打包，建议每次更新文章都重新构建镜像。
